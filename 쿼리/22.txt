			//메인 이미지 추가
			Image main = new ImageIcon("image/kimbok/9.png").getImage().getScaledInstance(350,250 ,0);
			Image back = new ImageIcon("image/kimbok/back.jpg").getImage().getScaledInstance(550,300 ,0);
			
			
			lbMain = new JLabel(new ImageIcon(main));
			lbback = new JLabel(new ImageIcon(back));
			
			panMain = new JPanel(); // 기본이미지
			panRecipe = new JPanel(); // 요리방법 이미지
			
			JPanel pan2 = new JPanel(); // 레시피 텍스트
			
			JTextArea textArea = new JTextArea(); 
			textArea = new JTextArea(15,20);
			
			
//			textArea.append(re.recipeArr());
			
			

//		    JFrame f = new JFrame("Hack the Planet");
		    JPanel panText = new JPanel(); 
//		    JPanel lowerPanel = new JPanel();
		    
   
		    panText.setBackground(Color.pink);

		    textArea.setEditable(false);



		    panText.add(textArea);
//		    lowerPanel.add(textField);
		 
//		    f.pack();
//		    f.setVisible(true);
//		    textField.requestFocus();
		    
			

			panMain.add(lbMain);
			panRecipe.add(lbback);
//			pan2.add(textArea);
			
			
			

			JButton btn1 = new JButton("레시피 보기");

			// 버튼 위치 조정
			btn1.setBounds(450, 500, 100, 30);

			// 레시피로 넘기기
			btn1.addActionListener(new ActionListener() {
				
				@Override
				public void actionPerformed(ActionEvent e) {
					// 해당 버튼이 클릭되었을 때
					// 보여주는 이미지 객체를 바꾸면 이미지가 변경된다.
					new newWindow();
					
					
				}
			});
			
			
			
	}
		class newWindow extends JFrame {
		    // 버튼이 눌러지면 만들어지는 새 창을 정의한 클래스
		    newWindow() {

		    	String[] datas = new String[] 

		    			{"recipe1","recipe2","recipe3","recipe4","recipe5","recipe6","recipe7","recipe8","recipe9"};

		    	JList<String> list = new JList<String>(); 

		    

		    		list.setListData(datas);  //리스트 객체의 설정과 항목들 설정

		    		list.setSelectionBackground(Color.YELLOW);

		    		list.setSelectionForeground(Color.RED);

		    		add(list,BorderLayout.WEST);

		    		

		    		JPanel panel = new JPanel();

//		    		panel.setLayout(new GridLayout(3, 2, 5, 5)); //row,col,hgap,vgap

		    	
		    		
		    		JLabel[] imgLabels = new JLabel[9]; //배열 객체 생성

		    		
		    		
		    		
		    	
		    		for(int i = 0; i < imgLabels.length; i++) {

		    			//이미지 아이콘 객체 생성

		    			ImageIcon icon = new ImageIcon("image/kimbok/"+ (i+1)+".png");
		    			
		    			icon = new ImageIcon( icon.getImage().getScaledInstance(600, 500,  0));//Resizing
//		    			if()) {
		    			imgLabels[i] = new JLabel(icon); // 

		    			panel.add(imgLabels[i]); //만들며 붙이기
//		    			}
		    		}
		    		
		    		

//		    		add(panel, BorderLayout.CENTER);
		    		add(panel);

		    		

		    		list.addListSelectionListener(new ListSelectionListener() {			

		    			@Override //리스트의 항목 선택시 자동 실행되는 메소드 : 콜백메소드

		    			public void valueChanged(ListSelectionEvent e) {

		    				//리스트에게 선택된 항목의 인덱스번호 얻어오기

		    				int index = list.getSelectedIndex();

		    				for(JLabel t : imgLabels) t.setVisible(false);

		    				imgLabels[index].setVisible(true);				

		    			}

		    		});		

		    		

		    		JButton btn = new JButton("처음으로");

		    		add(btn,BorderLayout.NORTH);

		    		btn.addActionListener(new ActionListener() {			

		    			@Override

		    			public void actionPerformed(ActionEvent e) {
		    				
		    				toStart="첫번째 화면";
//		    				change();
		    			}

		    		});

		    		

		    		setTitle("recipes");

		    		setLocation(600, 100);

		    		setDefaultCloseOperation(EXIT_ON_CLOSE);

		    		setSize(750, 550);

		    		setVisible(true);

		    	
		    
		    
		    }
		}
//		    
		
		
		
}